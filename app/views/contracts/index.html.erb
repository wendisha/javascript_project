<h1>Your Contract Investigations</h1>

<% if @contracts.length > 0 %>
<% @contracts.all.each do |contract| %>
<%= link_to "#{contract.player.name} & #{contract.club.name}", user_contract_path(contract.user, contract) %>
<% end %>
<% end %>
<br>
<%= link_to "Create a New Contract", new_user_contract_path(User.find_by_id(session[:user_id])) %>
